# GDP-Data-Extraction-and-Processing
This project demonstrates a data extraction and processing workflow using Python and the Pandas library. The script scrapes GDP data from a Wikipedia page, cleans and transforms the data, and saves the final output to a CSV file.
